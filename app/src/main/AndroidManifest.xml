<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a300277280.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainPage"
            android:label="@string/title_activity_main_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IteneraryPage" />
        <activity android:name=".onboardActivity" />
        <activity android:name=".tab5thlevel" />
        <activity android:name=".tab9thlevel" />
        <activity android:name=".tab16thlevel" />
        <activity android:name=".RoomPage" />
        <activity android:name=".InvoicePage" />
        <activity android:name=".Ports" />
        <activity android:name=".adminPage" />
        <activity
            android:name=".Juneau"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".Sitka"
            android:theme="@style/AppTheme.CustomTheme" />
    </application>

</manifest>